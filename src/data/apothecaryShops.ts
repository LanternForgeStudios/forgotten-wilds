/** Display-only client mirror of functions/src/data/apothecaryShops.ts - the client only needs to
 *  know WHICH shopIds offer the Restock Supplies dialogue option (the server generates/validates
 *  the actual quest, including which materials it can target). */
export const APOTHECARY_SHOP_IDS: string[] = ['apothecary', 'noelle-herbalist'];
